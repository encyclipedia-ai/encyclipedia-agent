# Optional. Defaults to https://api.encyclipedia.ai
# ENCYCLIPEDIA_API_URL=http://localhost:3001

# Optional. Defaults to the production Firebase web API key (public).
# ENCYCLIPEDIA_FIREBASE_API_KEY=

# Optional: Firebase Auth emulator, e.g. localhost:9099
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099
